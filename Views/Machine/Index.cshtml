@model IEnumerable<MakinaPeryodikBakimTakibi.Models.Machine>
@{
    ViewData["Title"] = "Makinalar";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h2>Makinalar</h2>
    <a asp-action="Create" class="btn btn-primary">Yeni Makina</a>
</div>

<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>Kod</th>
            <th>Ad</th>
            <th>Bölüm</th>
            <th>Marka</th>
            <th>Durum</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Code</td>
                <td>@item.Name</td>
                <td>@item.Department?.Name</td>
                <td>@item.Brand</td>
                <td>@(item.IsActive ? "Aktif" : "Pasif")</td>
            </tr>
        }
    </tbody>
</table>
